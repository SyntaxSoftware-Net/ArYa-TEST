import { Client } from '../Client';
import { ClientEvents } from 'discord.js';

interFace Run {
  (client: Client, ...args: any[])
  
  export interFace Event {
    name: keyif ClientEvents;
    run: Run;
    }